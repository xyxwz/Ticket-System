!!! html
html
  head
    title TxSSC Ticket System
    meta(name="viewport", content="width=device-width,initial-scale=1")

    - if (process.env.NODE_ENV === 'production')
      link(rel="stylesheet", href="/css/bundle.css")
    - else
      link(rel="stylesheet/less", type="text/css", href="/css/less/main.less")
      script(src="/js/libs/less-1.3.3.min.js", type="text/javascript").

  body.login

    h1.logo Texas School Safety Center

    .login-form
      p Tickets is a task management tool for groups, it helps you manage and collaborate on tasks.

      .btn-wrapper
        a.btn-auth.btn-txssc.large(href='/login/oauth/authorize', title='Sign in with TxSSC') Login